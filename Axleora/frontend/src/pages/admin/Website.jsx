import { useEffect,useState } from "react";
import { api } from "../../api/axios";
import { useSite } from "../../context/SiteContext";
const fields=[["businessName","Business name"],["phone","Workshop phone"],["email","Public email"],["address","Street address"],["mapQuery","Google Maps search location"],["openingHours","Opening hours"],["homeEyebrow","Homepage eyebrow"],["homeTitle","Homepage main heading"],["homeIntro","Homepage introduction"],["contactTitle","Contact page heading"],["contactIntro","Contact page introduction"]];
export default function Website(){const{site,setSite}=useSite();const[form,setForm]=useState({}),[busy,setBusy]=useState(false),[message,setMessage]=useState(""),[error,setError]=useState("");useEffect(()=>setForm(site),[site]);const save=async e=>{e.preventDefault();setBusy(true);setError("");setMessage("");try{const{data}=await api.put("/site",form);setSite(data);setForm(data);setMessage("Website content updated.")}catch(err){setError(err.response?.data?.message||"Website content could not be saved")}finally{setBusy(false)}};return <div className="admin-content"><div className="admin-title"><div><span className="kicker">WEBSITE CMS</span><h1>Website content</h1><p>Manage public business details, homepage messaging, contact content and map location.</p></div><a className="btn ghost" href="/" target="_blank" rel="noreferrer">Preview website</a></div>{error&&<div className="alert error">{error}</div>}{message&&<div className="alert success">{message}</div>}<form className="admin-panel website-editor" onSubmit={save}><div className="panel-head"><div><h2>Public information</h2><p>Changes are shown across the customer website.</p></div></div><div className="website-fields">{fields.map(([key,label])=><label className={["homeTitle","homeIntro","contactTitle","contactIntro"].includes(key)?"wide":""} key={key}>{label}{["homeIntro","contactIntro"].includes(key)?<textarea rows="3" value={form[key]||""} onChange={e=>setForm({...form,[key]:e.target.value})}/>:<input required value={form[key]||""} onChange={e=>setForm({...form,[key]:e.target.value})}/>}</label>)}</div><div className="website-map-preview"><iframe title="Google Maps preview" src={`https://www.google.com/maps?q=${encodeURIComponent(form.mapQuery||form.address||"Colombo 03")}&output=embed`} loading="lazy" referrerPolicy="no-referrer-when-downgrade"/></div><div className="editor-actions"><button className="btn" disabled={busy}>{busy?"Saving…":"Publish changes"}</button></div></form></div>}
